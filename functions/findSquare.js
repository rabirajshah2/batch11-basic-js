const findSquare = (val) => {
  const num = val * val;
  return num;
};
console.log(findSquare(5));

// const findSquare = (arr) => {
//     const num = arr.map((x) => x * x);
//     return num;
//   };
//   console.log(findSquare([2, 4, 5]));
